# GUI audit — software

**Portfolio index (sibling repo audits):** [MiscRepos `GUI_AUDIT_PORTFOLIO_INDEX.md`](../../../MiscRepos/docs/audit/GUI_AUDIT_PORTFOLIO_INDEX.md).

**Date:** 2026-03-26  
**Mode:** Read-only audit  

## GUI scope: N/A (this tree)

The **software** repository as present in the workspace is **plans-heavy** (e.g. `plans/*.plan.md`) without a root `package.json` or primary web application. Actual app UIs (e.g. OpenAtlas, Arc_Forge `workflow_ui`) live in **sibling repositories** — audit those artifacts instead.

## If a web app exists in a subfolder later

Re-run the multi-repo playbook **Step 0**: glob for `package.json`, `next.config.*`, Flask `app.py`, Gradio, then apply the standard template to that subpath.

## E2E

N/A at repo root.

## Definition of done

- [x] N/A + redirect to sibling app repos  
