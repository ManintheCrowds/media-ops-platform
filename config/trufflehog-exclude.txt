# TruffleHog path exclude patterns (one regex per line).
# Scheduled full-history scan only — see security-trufflehog.yml.
# Dev compose placeholders (${*_POSTGRES_PASSWORD:-changeme}) — NOT production.
# docker-compose.prod.yml is intentionally NOT listed.
^docker-compose\.yml$
^docker-compose\.vlans\.yml$
^job-automation-service/docker-compose\.yml$
^education-service/docker-compose\.yml$
^home-cyber-risk/infra/docker-compose\.yml$
